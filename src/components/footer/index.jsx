import styles from './styles.module.scss';

export default function Footer() {
    return (
        <footer className={styles.footer}>
            <p>made with love by <strong>@gus.az</strong></p>
        </footer>
    );
}